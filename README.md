Sources for the plugin `plugin.appodeal.Mobvista`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.Mobvista" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
